import { MovieSearch } from "../components/MovieSearch";

const Movies = () => {
    // const movies = getMovies();
    return (
        <main>
            <h1>Movies</h1>
            <MovieSearch />
        </main>
    );
};

export default Movies;
