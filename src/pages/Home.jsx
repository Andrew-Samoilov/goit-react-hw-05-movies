import { MoviesList } from "../components/MoviesList";

const Home = () => {
    return (
        <main>
            <h1>Trending today</h1>
            <MoviesList/>
        </main>
    );
};

export default Home;
